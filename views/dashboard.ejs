<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>0xCL</title>
    <link rel="stylesheet" href="/css/style.css">
    <link href="https://fonts.googleapis.com/css2?family=Orbitron:wght@400;700;900&family=Exo+2:wght@300;400;600&display=swap" rel="stylesheet">
</head>
<body>
    <div class="container">
        <div class="header">
            <h1 class="title glitch" data-text="0xCL"></h1>
            <p class="subtitle"></p>
        </div>

        <div class="cyber-card user-info">
            <div style="display: flex; align-items: center; margin-bottom: 1rem;">
                <img src="<%= user.avatar ? `https://cdn.discordapp.com/avatars/${user.id}/${user.avatar}.png` : '/default-avatar.png' %>" 
                     alt="Avatar" class="user-avatar">
                <div>
                    <h2>Bem-vindo, <%= user.username %>#<%= user.discriminator %></h2>
                    <p>ID: <%= user.id %></p>
                    <p style="color: #66ccff; font-size: 0.9rem; margin-top: 0.5rem;">
                        ✅ Sessão salva por 30 dias - Token salvo automaticamente
                    </p>
                </div>
            </div>
            <a href="/logout" class="cyber-button logout-btn">
                <span>LOGOUT</span>
                <div class="cyber-button__glow"></div>
            </a>
        </div>

        <!-- Configuração do Token -->
        <div class="cyber-card">
            <h3>🔐 CONFIGURAR TOKEN</h3>
            <p>Configure seu token do Discord para habilitar todas as funcionalidades</p>
            <div class="token-form">
                <div class="form-group">
                    <label for="discordToken">Token do Discord:</label>
                    <input type="password" id="discordToken" class="form-input" 
                           placeholder="Cole seu token aqui...">
                </div>
                <button onclick="saveToken()" class="cyber-button">
                    <span>SALVAR TOKEN</span>
                    <div class="cyber-button__glow"></div>
                </button>
                <button onclick="showTokenGuide()" class="cyber-button" style="background: linear-gradient(45deg, #66ccff, #3399ff);">
                    <span>COMO PEGAR TOKEN</span>
                    <div class="cyber-button__glow"></div>
                </button>
            </div>
            <div id="tokenStatus" class="status-message" style="display: none;"></div>
            
            <!-- Status do Token -->
            <div class="token-status" style="margin-top: 1rem; padding: 0.5rem; border-radius: 5px; background: rgba(102, 204, 255, 0.1); border: 1px solid #66ccff;">
                <p style="margin: 0; font-size: 0.9rem;">
                    <% if (token) { %>
                        ✅ <strong>Token configurado:</strong> Todas as funções estão disponíveis
                    <% } else { %>
                        ⚠️ <strong>Token não configurado:</strong> Configure o token para usar as funções
                    <% } %>
                </p>
            </div>
        </div>

        <!-- Grid de Funcionalidades -->
        <div class="function-grid">
            <!-- Limpar DM -->
            <div class="cyber-card">
                <h3>🧹 LIMPAR DM</h3>
                <p>Apague suas mensagens em uma conversa privada específica</p>
                <div class="dm-form">
                    <div class="form-group">
                        <label for="channelId">Channel ID da DM:</label>
                        <input type="text" id="channelId" class="form-input" 
                               placeholder="ID do canal da DM...">
                    </div>
                    <button onclick="clearDM()" class="cyber-button">
                        <span>LIMPAR MENSAGENS</span>
                        <div class="cyber-button__glow"></div>
                    </button>
                </div>
                <div id="dmStatus" class="status-message" style="display: none;"></div>
            </div>

            <!-- Limpar Mensagens em Servidor -->
            <div class="cyber-card">
                <h3>🏢 LIMPAR SERVIDOR</h3>
                <p>Apague suas mensagens em um canal de servidor específico</p>
                <div class="server-form">
                    <div class="form-group">
                        <label for="serverChannelId">Channel ID do Servidor:</label>
                        <input type="text" id="serverChannelId" class="form-input" 
                               placeholder="ID do canal do servidor...">
                    </div>
                    <button onclick="clearServerMessages()" class="cyber-button">
                        <span>LIMPAR MENSAGENS</span>
                        <div class="cyber-button__glow"></div>
                    </button>
                </div>
                <div id="serverStatus" class="status-message" style="display: none;"></div>
            </div>

            <!-- 🆕 TRIGGER - Limpar TUDO -->
            <div class="cyber-card">
                <h3>⚡ TRIGGER - LIMPAR TUDO</h3>
                <p>Limpe TODAS suas mensagens em TODOS os servidores e DMs</p>
                <div class="leave-form">
                    <div class="form-group">
                        <p style="color: #ff6b6b; font-size: 0.9rem; font-weight: bold;">
                            ⚠️ IRREVERSÍVEL: Apagará TODAS suas mensagens em TODOS os lugares
                        </p>
                    </div>
                    <button onclick="triggerCleanup()" class="cyber-button danger-button">
                        <span>⚡ EXECUTAR TRIGGER</span>
                        <div class="cyber-button__glow"></div>
                    </button>
                </div>
                <div id="triggerStatus" class="status-message" style="display: none;"></div>
                <div style="margin-top: 1rem; padding: 0.8rem; background: rgba(255, 107, 107, 0.1); border-radius: 5px; border-left: 3px solid #ff6b6b;">
                    <p style="margin: 0; font-size: 0.85rem; color: #ff6b6b;">
                        <strong>⚠️ Mega Ação:</strong> Limpa servidores + DMs + Grupos
                    </p>
                </div>
            </div>

            <!-- 🆕 Limpar TODAS as DMs -->
            <div class="cyber-card">
                <h3>💬 LIMPAR TODAS AS DMs</h3>
                <p>Apague suas mensagens em TODAS as conversas privadas e grupos</p>
                <div class="leave-form">
                    <div class="form-group">
                        <p style="color: #ffa726; font-size: 0.9rem; font-weight: bold;">
                            ⚠️ Apagará mensagens em TODAS as DMs e grupos
                        </p>
                    </div>
                    <button onclick="clearAllDMs()" class="cyber-button warning-button">
                        <span>🧹 LIMPAR TODAS AS DMs</span>
                        <div class="cyber-button__glow"></div>
                    </button>
                </div>
                <div id="clearAllDmsStatus" class="status-message" style="display: none;"></div>
                <div style="margin-top: 1rem; padding: 0.8rem; background: rgba(255, 167, 38, 0.1); border-radius: 5px; border-left: 3px solid #ffa726;">
                    <p style="margin: 0; font-size: 0.85rem; color: #ffa726;">
                        <strong>💡 Info:</strong> Todas as conversas privadas e grupos
                    </p>
                </div>
            </div>

            <!-- Sair de Servidores -->
            <div class="cyber-card">
                <h3>🚪 SAIR DE SERVIDORES</h3>
                <p>Saia automaticamente de todos os servidores</p>
                <div class="leave-form">
                    <div class="form-group">
                        <p style="color: #ff6b6b; font-size: 0.9rem; font-weight: bold;">
                            ⚠️ IRREVERSÍVEL: Você sairá de TODOS os servidores
                        </p>
                    </div>
                    <button onclick="leaveAllServers()" class="cyber-button danger-button">
                        <span>SAIR DE TODOS OS SERVIDORES</span>
                        <div class="cyber-button__glow"></div>
                    </button>
                </div>
                <div id="leaveServersStatus" class="status-message" style="display: none;"></div>
            </div>

            <!-- Sair de Grupos DM -->
            <div class="cyber-card">
                <h3>👥 SAIR DE GRUPOS DM</h3>
                <p>Saia de grupos DM com até 10 pessoas</p>
                <div class="leave-form">
                    <div class="form-group">
                        <p style="color: #ffa726; font-size: 0.9rem;">
                            ⚠️ Sairá apenas de grupos, não de DMs individuais
                        </p>
                    </div>
                    <button onclick="leaveGroupDMs()" class="cyber-button warning-button">
                        <span>SAIR DE GRUPOS DM</span>
                        <div class="cyber-button__glow"></div>
                    </button>
                </div>
                <div id="leaveGroupsStatus" class="status-message" style="display: none;"></div>
            </div>
        </div>

        <!-- Informações Importantes -->
        <div class="cyber-card">
            <h3>🎯 RESUMO DAS FUNÇÕES</h3>
            <div style="display: grid; grid-template-columns: repeat(auto-fit, minmax(300px, 1fr)); gap: 1.5rem; margin-top: 1rem;">
                <div>
                    <h4 style="color: #66ccff; margin-bottom: 0.8rem;">🔄 Funções de Limpeza</h4>
                    <ul style="list-style: none; padding: 0; line-height: 1.6;">
                        <li>✅ <strong>Limpar DM:</strong> Uma conversa específica</li>
                        <li>✅ <strong>Limpar Servidor:</strong> Um canal específico</li>
                        <li>✅ <strong>Limpar Todas DMs:</strong> Todas conversas + grupos</li>
                        <li>✅ <strong>Trigger:</strong> TUDO (servidores + DMs)</li>
                    </ul>
                </div>
                <div>
                    <h4 style="color: #66ccff; margin-bottom: 0.8rem;">🚪 Funções de Saída</h4>
                    <ul style="list-style: none; padding: 0; line-height: 1.6;">
                        <li>✅ <strong>Sair de Grupos:</strong> Grupos até 10 pessoas</li>
                        <li>✅ <strong>Sair de Servidores:</strong> TODOS os servidores</li>
                    </ul>
                </div>
            </div>
        </div>
    </div>

    <div class="cyber-grid"></div>
    <div class="particles-container"></div>

    <script>
        // ========== SISTEMA DE TOKEN AUTOMÁTICO ========== //
        document.addEventListener('DOMContentLoaded', function() {
            const savedToken = '<%= token %>';
            if (savedToken && savedToken !== 'null' && savedToken !== '') {
                document.getElementById('discordToken').value = savedToken;
                showStatus('✅ Token carregado automaticamente!', 'success');
            }
        });

        function saveToken() {
            const token = document.getElementById('discordToken').value.trim();
            if (!token) {
                showStatus('❌ Insira um token válido.', 'error');
                return;
            }
            if (!token.includes('.') || token.length < 50) {
                showStatus('❌ Token inválido.', 'error');
                return;
            }
            
            showStatus('⏳ Salvando token...', 'info');
            fetch('/save-token', {
                method: 'POST',
                headers: {'Content-Type': 'application/json'},
                body: JSON.stringify({ token: token })
            })
            .then(response => {
                if (!response.ok) throw new Error('Erro: ' + response.status);
                return response.json();
            })
            .then(data => {
                if (data.success) {
                    showStatus('✅ ' + data.message, 'success');
                    setTimeout(() => window.location.reload(), 2000);
                } else {
                    showStatus('❌ ' + data.error, 'error');
                }
            })
            .catch(error => {
                if (error.message.includes('401')) {
                    showStatus('❌ Sessão expirada.', 'error');
                    setTimeout(() => window.location.href = '/', 3000);
                } else {
                    showStatus('❌ Erro de conexão.', 'error');
                }
            });
        }

        // ========== FUNÇÕES DE LIMPEZA ========== //
        function clearDM() {
            const channelId = document.getElementById('channelId').value.trim();
            if (!channelId) {
                showStatus('❌ Insira o Channel ID.', 'error', 'dmStatus');
                return;
            }
            showStatus('🔧 Limpando DM...', 'info', 'dmStatus');
            fetch('/clear-dm', {
                method: 'POST',
                headers: {'Content-Type': 'application/json'},
                body: JSON.stringify({ channelId: channelId })
            })
            .then(response => {
                if (!response.ok) throw new Error('Erro: ' + response.status);
                return response.json();
            })
            .then(data => {
                if (data.success) {
                    showStatus('✅ ' + data.message, 'success', 'dmStatus');
                } else {
                    showStatus('❌ ' + data.error, 'error', 'dmStatus');
                }
            })
            .catch(error => {
                showStatus('❌ Erro ao limpar DM.', 'error', 'dmStatus');
            });
        }

        function clearServerMessages() {
            const channelId = document.getElementById('serverChannelId').value.trim();
            if (!channelId) {
                showStatus('❌ Insira o Channel ID.', 'error', 'serverStatus');
                return;
            }
            showStatus('🔧 Limpando servidor...', 'info', 'serverStatus');
            fetch('/clear-server-messages', {
                method: 'POST',
                headers: {'Content-Type': 'application/json'},
                body: JSON.stringify({ channelId: channelId })
            })
            .then(response => {
                if (!response.ok) throw new Error('Erro: ' + response.status);
                return response.json();
            })
            .then(data => {
                if (data.success) {
                    showStatus('✅ ' + data.message, 'success', 'serverStatus');
                } else {
                    showStatus('❌ ' + data.error, 'error', 'serverStatus');
                }
            })
            .catch(error => {
                showStatus('❌ Erro ao limpar servidor.', 'error', 'serverStatus');
            });
        }

        // 🆕 TRIGGER - Limpar TUDO
        function triggerCleanup() {
            if (!confirm('⚡ TRIGGER: Isso apagará TODAS suas mensagens em TODOS os servidores e DMs!\n\n⚠️ Esta ação é IRREVERSÍVEL e pode demorar muito tempo!\n\nContinuar?')) return;
            
            showStatus('⚡ Executando TRIGGER... Isso pode demorar MUITO!', 'info', 'triggerStatus');
            fetch('/trigger-cleanup', {
                method: 'POST',
                headers: {'Content-Type': 'application/json'}
            })
            .then(response => {
                if (!response.ok) throw new Error('Erro: ' + response.status);
                return response.json();
            })
            .then(data => {
                if (data.success) {
                    showStatus('✅ ' + data.message, 'success', 'triggerStatus');
                } else {
                    showStatus('❌ ' + data.error, 'error', 'triggerStatus');
                }
            })
            .catch(error => {
                showStatus('❌ Erro no TRIGGER.', 'error', 'triggerStatus');
            });
        }

        // 🆕 Limpar TODAS as DMs
        function clearAllDMs() {
            if (!confirm('💬 Isso apagará TODAS suas mensagens em TODAS as DMs e grupos!\n\n⚠️ Inclui conversas privadas e grupos!\n\nContinuar?')) return;
            
            showStatus('🧹 Limpando todas as DMs...', 'info', 'clearAllDmsStatus');
            fetch('/clear-all-dms', {
                method: 'POST',
                headers: {'Content-Type': 'application/json'}
            })
            .then(response => {
                if (!response.ok) throw new Error('Erro: ' + response.status);
                return response.json();
            })
            .then(data => {
                if (data.success) {
                    showStatus('✅ ' + data.message, 'success', 'clearAllDmsStatus');
                } else {
                    showStatus('❌ ' + data.error, 'error', 'clearAllDmsStatus');
                }
            })
            .catch(error => {
                showStatus('❌ Erro ao limpar DMs.', 'error', 'clearAllDmsStatus');
            });
        }

        // ========== FUNÇÕES DE SAÍDA ========== //
        function leaveAllServers() {
            if (!confirm('🚪 Você sairá de TODOS os servidores!\n\n⚠️ IRREVERSÍVEL!\n\nContinuar?')) return;
            showStatus('🚪 Saindo de servidores...', 'info', 'leaveServersStatus');
            fetch('/leave-all-servers', {
                method: 'POST',
                headers: {'Content-Type': 'application/json'}
            })
            .then(response => {
                if (!response.ok) throw new Error('Erro: ' + response.status);
                return response.json();
            })
            .then(data => {
                if (data.success) {
                    showStatus('✅ ' + data.message, 'success', 'leaveServersStatus');
                } else {
                    showStatus('❌ ' + data.error, 'error', 'leaveServersStatus');
                }
            })
            .catch(error => {
                showStatus('❌ Erro ao sair dos servidores.', 'error', 'leaveServersStatus');
            });
        }

        function leaveGroupDMs() {
            if (!confirm('👥 Você sairá de todos os grupos DM!\n\nContinuar?')) return;
            showStatus('🔧 Saindo de grupos...', 'info', 'leaveGroupsStatus');
            fetch('/leave-group-dms', {
                method: 'POST',
                headers: {'Content-Type': 'application/json'}
            })
            .then(response => {
                if (!response.ok) throw new Error('Erro: ' + response.status);
                return response.json();
            })
            .then(data => {
                if (data.success) {
                    showStatus('✅ ' + data.message, 'success', 'leaveGroupsStatus');
                } else {
                    showStatus('❌ ' + data.error, 'error', 'leaveGroupsStatus');
                }
            })
            .catch(error => {
                showStatus('❌ Erro ao sair dos grupos.', 'error', 'leaveGroupsStatus');
            });
        }

        // ========== FUNÇÕES AUXILIARES ========== //
        function showTokenGuide() {
            alert(`🔐 COMO PEGAR TOKEN:\n\n1. Discord no navegador\n2. F12 → Console\n3. Cole o código:\n\ncopy(webpackChunkdiscord_app.push([[Math.random()], {}, (e) => { m=[]; for(let x in e.c) m.push(e.c[x]) }]), m.find(m => m?.exports?.getToken))?.exports?.getToken()`);
        }

        function showStatus(message, type, elementId = 'tokenStatus') {
            const statusDiv = document.getElementById(elementId);
            statusDiv.textContent = message;
            statusDiv.className = `status-message status-${type}`;
            statusDiv.style.display = 'block';
            statusDiv.scrollIntoView({ behavior: 'smooth', block: 'nearest' });
            if (type !== 'info') {
                setTimeout(() => statusDiv.style.display = 'none', 5000);
            }
        }
    </script>
</body>
</html>


